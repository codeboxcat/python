raise 语句
assert 语句
迭代器 和 生成器
netx(it)
iter(obj)
StopIteration 异常

生成器：
生成器函数：
  yield语句 每次调用该语句会生成一个数据，该数据能够被迭代器拿到。
  range()和xrange() 在2.0中都有，但是在3.0中只有保留range()
  range()这个函数是现用现生成的，它占用的空间很小。用时间换空间，来提高存储效率。
  降低内存消耗
  L=[x for x in range(1,10000)]
  for i x ,in enumerate(range(1,10000)):
    print(i,x)
  #用生成器函数来做
  for i,x in enumerate(range(1,10000))
    print(i,x)
生成器表达式：
  (x for x in [1,2,3])
  
 
  
